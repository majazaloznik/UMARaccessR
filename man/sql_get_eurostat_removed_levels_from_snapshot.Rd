% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metabase.R
\name{sql_get_eurostat_removed_levels_from_snapshot}
\alias{sql_get_eurostat_removed_levels_from_snapshot}
\title{Get removed levels for datasets in a Eurostat metabase snapshot}
\usage{
sql_get_eurostat_removed_levels_from_snapshot(
  con,
  snapshot_id,
  datasets,
  schema = "eurostat"
)
}
\arguments{
\item{con}{Database connection object}

\item{snapshot_id}{Integer (or integer64) snapshot identifier}

\item{datasets}{Character vector of dataset codes to report on}

\item{schema}{Character string specifying the database schema}
}
\value{
A data frame with columns `dataset`, `dim`, and `removed`
  (comma-separated codes, alphabetical); zero rows if no datasets were
  supplied or none of them had removed levels in this snapshot.
}
\description{
Returns the specific position codes removed (not just counts) for the given
datasets at the given snapshot, so alert emails can list exactly which levels
disappeared. A removed level is the actionable case: it breaks queries that
filter on it. The `time` dimension is excluded.
}
\details{
Wraps the `eurostat.get_removed_levels` database function, which takes the
dataset list as a native text array — no client-side quoting required.
}
