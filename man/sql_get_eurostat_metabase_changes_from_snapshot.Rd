% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metabase.R
\name{sql_get_eurostat_metabase_changes_from_snapshot}
\alias{sql_get_eurostat_metabase_changes_from_snapshot}
\title{Get Eurostat metabase structural changes for a snapshot}
\usage{
sql_get_eurostat_metabase_changes_from_snapshot(
  con,
  snapshot_id,
  schema = "eurostat"
)
}
\arguments{
\item{con}{Database connection object}

\item{snapshot_id}{Integer (or integer64) snapshot identifier}

\item{schema}{Character string specifying the database schema}
}
\value{
A data frame with columns `dataset`, `event`, `dim_changes`,
  `level_added`, `level_removed`; zero rows if the snapshot introduced no
  (non-time) structural changes.
}
\description{
Returns one row per affected dataset for the given metabase snapshot,
classifying each change as a whole-dataset event (`dataset_added` /
`dataset_removed`) or a within-dataset change (`changed`) with dimension
changes and level (position) counts. The `time` dimension is excluded.
}
\details{
Wraps the `eurostat.get_metabase_changes` database function.
}
