% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metabase.R
\name{sql_resolve_eurostat_folder_datasets}
\alias{sql_resolve_eurostat_folder_datasets}
\title{Resolve a Eurostat TOC folder code to its descendant dataset/table codes}
\usage{
sql_resolve_eurostat_folder_datasets(con, folder_code, schema = "eurostat")
}
\arguments{
\item{con}{Database connection object}

\item{folder_code}{Character folder code (from the TOC navigation tree)}

\item{schema}{Character string specifying the database schema}
}
\value{
A data frame with columns `code` and `type` ('dataset' or 'table');
  zero rows if the folder is unknown or empty.
}
\description{
Walks the current TOC tree from the given folder code and returns every
descendant leaf (dataset or table) beneath it, transitively. Folders are not
returned. Used to expand a folder subscription into the set of dataset codes
it should match against. A folder that does not exist, or contains no leaves,
returns zero rows.
}
\details{
Wraps the `eurostat.resolve_folder_datasets` database function.
}
